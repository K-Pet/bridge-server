# The frontend does NOT use environment variables for Supabase credentials.
# It fetches them at runtime from the Go backend via GET /api/config.
#
# No VITE_* vars are needed — configure the backend's .env.local instead.
